sudo: required

services:
  - docker

branches:
  only:
    - deployment

before_install:
  - docker network create hotel-network

script:
  - docker-compose up --build

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push mhmdmaani/hotel-booking:"1.0.0"
